;; Where would a `require` statement go in this Clojure code?
(ns falcon.core
  (:require '[etaoin.api :as e]
            '[etaoin.keys :as k]
            '[etaoin.string :as str]))

(defn -main [& args]
  ())
